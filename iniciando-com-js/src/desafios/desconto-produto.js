// Calcular o preço com descontos de 30%

let preco = 129.9;
let desconto = 0.3;

let precoComDesconto = preco * (1 - desconto);

console.log("O preço com desconto é: " + precoComDesconto.toFixed(2));
